const add = (a,b) => {
  return a + b
}

export const sub = (a,b) => {
  return a - b
}

export const multiply = (a, b) => {
  return a * b;
}

export const divide = (a, b) => {
  return a / b;
}

export default add;
// export {sub, multiply, divide}